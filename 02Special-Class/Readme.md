## sigma Crud | special Class

```
npm i express mongoose dotenv cors
```

#### what is cors
- Cross-origin resource sharing (CORS) is a browser mechanism which enables controlled access to resources located outside of a given domain.